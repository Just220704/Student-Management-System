# MySQL Connection
# Change 'update' to 'none' after your tables are perfectly set up to prevent Hibernate from damaging constraints
spring.datasource.url=jdbc:mysql://localhost:3306/student_management
spring.datasource.username=root
spring.datasource.password=Justin@04

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# NEW: Initialize the N-Admin schema script
spring.sql.init.mode=always

# --- JavaMailSender Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=fernandesjustin940@gmail.com
# ALWAYS use a Google App Password, but hide it before pushing to GitHub
spring.mail.password=vshv nhzf mbku vztu 

# Standard SMTP Security Protocols
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true